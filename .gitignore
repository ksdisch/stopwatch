.DS_Store

# Claude Code — track orchestrator/agents/templates; ignore personal state
.claude/settings.local.json
.claude/worktrees/
.claude/launch.json

# Capacitor / Node
node_modules/
www/
ios/App/Pods/
ios/App/build/
ios/DerivedData/
ios/App/Podfile.lock
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/

# Firebase Admin / service-account keys (NEVER commit)
service-account.json
*-firebase-adminsdk-*.json
.firebaserc
# Firestore emulator artifact (npm run test:rules)
firestore-debug.log

# Life-OS council (Tier-2 local runtime) — deps, secrets, logs (NEVER commit)
council/node_modules/
council/.env.secrets
council/logs/
# Tier-3 weekly reflections (personal — NEVER commit; .gitkeep keeps the dir tracked)
council/data/reflections/*.json

# Local browser-verification scratch (Playwright MCP output, ad-hoc screenshots)
.playwright-mcp/
/home-hub-needs-attention.png

# Tempo Proving Ground (@playwright/test) — regenerated each run, never commit
test-results/
playwright-report/

# Backlog Hygiene — Decision Brief — 2026-07-19

First `/backlog-hygiene` run. **Steering (Gate 1):** Full scope · short arc (1–3 items) ·
balanced solidify-vs-build lean · **off-limits: iPhone-in-hand work + sync internals**
(Kyle-input-gated items stayed in play). **Decision (Gate 2):** Arc A picked; all proposed
hygiene edits approved and applied in the same docs-only PR as this brief.

Corpus inventoried: CLAUDE.md feature table + `docs/BACKLOG.md` (summary, candidate menu,
Tooling & DX, tech debt) · `docs/lifeos/roadmap.md` · `docs/bug-hunt/2026-07-07-full-project.md`
register · `docs/SESSION-LOG.md` · `docs/artifacts-plan.md` · open GitHub issues/PRs (zero of
each) · `git log` through `dd1900d` (2026-07-17).

## Hygiene findings (every verdict verified against HEAD this session)

### Stale / done-already / superseded → edits applied

| Item | Verdict | Evidence | Disposition applied |
|---|---|---|---|
| Row #22 Finances (both tables) | stale | Tables said "build pending" / "merge pending"; #198 (`66d972e`) live since 2026-07-08; council nightly logs through `2026-07-19T11-00-01Z` | Truth-up: LIVE; close-out = Kyle's July numbers → nightly council → verify |
| Row #3 native sync detail | done-already, misfiled | Listener parity #209 (`400eccf`); CAS proven impossible (permanent); detail sat under "Unshipped (forward-looking)" | Moved to Shipped post-mortems with dated moved-note |
| Candidate: BFRB Slice B | part-shipped | `js/bfrb-risk.js` (headed "Slice B") + stress nudge `js/global-bfrb.js:244–328` shipped in Life-OS P3; `grep debrief js/` → 0 | Verdict cell rewritten: live remainder = post-countdown debrief only |
| Candidate: Weekly Review + doctor report | fresh, narrowed | Recap half covered by Home hub council recap (P1, `js/home-ui.js`); doctor-ready export unshipped | Narrowed note added; export wedge picked as Arc A ③ |
| Candidate: Health Intelligence Engine | superseded | Consolidation role now server-side in council synthesis + client Tempo Coach | Retired (menu annotation + Parked/Retired entry) |
| Candidate: Tempo Presence | superseded | LA core shipped natively #201–#211; widgets/Siri/web-wedge never pursued | Retired; remainder noted as #4 follow-ups |
| Tooling: `/new-synced-store` command | diminished | Recipe executed by hand twice since (mood #145, finances #198); no 9th client store on the roadmap (P4 = server-side marts) | Retired |
| Rows #7 voice control / #8 group timing | dormant / structurally blocked | #7 Low-impact, untouched since the stopwatch era; #8 needs a backend the PWA deliberately lacks | Parked (blocks moved to § Parked / Retired) |
| `docs/artifacts-plan.md` (2026-05-30) | done-already | ADRs, runbooks, data dictionary, diagrams, ARCHITECTURE.md all exist at HEAD | Dated "largely delivered — historical" status line |
| SESSION-LOG missing the #211 session | gap (F4 recurrence) | `grep '#211' SESSION-LOG.md` → 0; last header was 2026-07-10 N-1; #211 merged 2026-07-11 | Backfilled entry (marked backfilled) |

### Verified still-real (ranked; no drift)

| Item | Verdict | Evidence |
|---|---|---|
| Branch protection on `main` | fresh — quick win | `gh api …/branches/main/protection` → 404 today; CI is PR-only, direct push deploys unchecked |
| CLAUDE.md headroom | fresh — **past-due** | 48,602 chars vs 40k budget; item's own trigger was ">39.5k" |
| `.claude/settings.json` allowlist | fresh — quick win | File carries only `hooks`; `/fewer-permission-prompts` automates the fix |
| Proving Ground slice 3 (spec per high-risk render seam) | fresh | Slices 1–2 shipped (#196/#197); hunt: "single biggest quality lever" |
| Headless-flake real fix | fresh (caution) | Adjudication crutch still documented; fix may brush sync-engine test seams (borderline vs exclusion) |
| Med Runway & Adherence | fresh | Supply badge (derived remaining) ≠ runway forecast/streaks |
| #4 LA remaining engines (Stopwatch/Interval/Cooking) | fresh | Stopwatch needs a count-up ContentState variant |
| #6 split-screen v2 (independent controls) | fresh | V1 (`js/compare-ui.js`) shipped; fuller vision open |
| HealthKit bridge | fresh, sequenced-later | Per its own menu row |
| Life-OS P4 / P5 remainder | fresh, too-big | P4 blocked on sibling repos publishing marts; P5 wedge pick needs `docs/lifeos/pillars.md` pass; #198 is the slice template |

### Parked (excluded lanes / Kyle-only) — consolidated in BACKLOG § Parked / Retired

Device-confirm queue (4 checks, one ~20-min phone session): #211 device-owner checks ·
sign-out R10 verify · ambient-audio check (open since 2026-05-26) · check-8 toggle re-arm.
Sync cluster (D1 per-field LWW + structural #1/#2): deferred-by-design, trigger = next sync
touch. App Store paperwork: Kyle-gated. Branch cleanup: 57 local / 13 remote today, Bash
blocks `branch -D`.

## Candidate arcs

- **Arc A — Close the books + one clean build (PICKED).** ① Finances close-out (Kyle's
  ~10-min July numbers at `#/life-building` → nightly council → verify + flip row #22 to
  Shipped). ② Guardrails trio: branch-protection rule · `/fewer-permission-prompts`
  allowlist · `/trim-context` on the past-due CLAUDE.md — zero app code. ③ Doctor-ready
  report slice: new pure engine reading History/meds/rest_log + export via the existing
  Export module; no sync-module edits, no device.
- **Arc B — Quality lever (not picked).** Proving Ground slice 3 + headless-flake fix +
  guardrails. Pure solidify; flake fix risks the sync exclusion.
- **Arc C — LA engines sim-only (not picked).** Would grow the parked device-confirm pile;
  revisit after one phone session clears the queue.

## Coverage note

Pipeline mix after grooming is healthy (build-new: doctor report, Med Runway, LA engines,
split-screen v2, HealthKit, P4/P5 · solidify: slice 3, flake, guardrails · UX polish:
legitimately quiet). Two flags, no invented items: **(1)** the entire device-confirm lane is
parked by exclusion — batch it into one ~20-min phone session before it calcifies. **(2)**
Idea inflow is thinning: the 2026-06-05 menu is half-consumed and `docs/ideas/` doesn't
exist — when it thins further, `/replenish` or `/brainstorm` (QuickWin/Moonshot) is the
restock path.

## Arc A starter prompt (routed handoff)

**Step 0 (Kyle, before or at session start, ~10 min):** open
`https://ksdisch.github.io/stopwatch/#/life-building` and enter your July finance numbers in
the monthly capture form (they sync as the `finances` store; the nightly council run at
~11:00Z picks them up automatically).

Then paste into a fresh session:

```
Read docs/backlog-hygiene/2026-07-19.md (Arc A). Work items ① and ② this session:

① Finances close-out (backlog #22): confirm my July numbers landed in the `finances`
   Firestore store (firebase MCP, read-only), then either wait for the next nightly
   council run or — with my go-ahead in-session — trigger one production run via
   council/run-synthesis.sh (production-intended, NOT a test; never npm-test the
   writers). Verify users/{uid}/synthesis/life_building refreshed and #/life-building
   renders real data (app-verifier fresh-context recipe). Then flip backlog row #22 to
   Shipped in CLAUDE.md + docs/BACKLOG.md via the usual docs PR.

② Guardrails trio (BACKLOG Tooling & DX): (a) add the branch-protection rule on main
   requiring the six CI checks (gh api PUT — show me the payload before applying);
   (b) run /fewer-permission-prompts and commit the allowlist; (c) run /trim-context —
   CLAUDE.md is 48.6k vs the 40k budget, past its own re-trim trigger.

When both are done, kick item ③ to its own run:
/autonomous-milestone Doctor-ready report slice — the narrowed Weekly Review candidate
(docs/BACKLOG.md candidate menu, 2026-06-05): a pure string-producing engine reading
History + meds + rest_log locally, exporting a portable doctor-prep summary via the
existing Export module. No synced-store writes, no native code, no sync-module edits.

Constraints for the arc: no iPhone-in-hand work, no sync-engine/merge-module internals
(both per hygiene 2026-07-19). The device-confirm queue stays parked in
docs/BACKLOG.md § Parked / Retired.
```

**Why routed this way:** ① is verification/close-out and ② needs Kyle-adjacent consents
(GitHub settings, permission grants) — both fit one supervised session; ③ is a
well-bounded, self-verifiable build → `/autonomous-milestone` per the hygiene skill's
routing table.
